<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Pizza Shop</title>
  </head>
  <body>
    <h2>Your cart</h2>

    <table class="table table-striped">
      <thead>
        <tr>
          <th scope="col">Name</th>
          <th scope="col">Price</th>
          <th scope="col">Qty</th>
        </tr>
      </thead>
      <tbody>
        <% tota_price = 0 %>
        <% tota_qty = 0 %>
        <% @items.each do |item| %>
        <tr>
          <td><% item[0].title %></td>
          <td><% item[0].price %></td>
          <td><% item[1] %></td>
        </tr>
        <% tota_price += item[0].price * item[1].to_i %>
        <% tota_price += item[1].to_i %>
        <% end %>

        <tr>
          <td>Total</td>
          <td><% tota_price %> rub</td>
          <td><% tota_qty %></td>
        </tr>

      </tbody>
    </table>

  </body>
</html>
